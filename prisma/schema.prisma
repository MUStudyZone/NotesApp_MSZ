// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["mongoDb", "dataProxy"]
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://AryTiwa:64646464At@cluster0.jutpn.mongodb.net/test?retryWrites=true&w=majority"
}

model Subject {
  id     String @id @default(auto()) @map("_id") @db.ObjectId
  name   String @unique
  branch String
  sem    Int
}

model Topic {
  id      String  @id @default(auto()) @map("_id") @db.ObjectId
  name    String
  url     String  @unique
  branch  String
  subject String
  desc    String?
}
